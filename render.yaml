services:
  - type: web
    name: edgescout
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: KALSHI_API_KEY
        sync: false
      - key: PORT
        value: 10000
